# Runtime deps for the streaming invoke Lambda (feature: responseStreaming).
# Kept minimal — boto3 for AgentCore Runtime calls, FastAPI+uvicorn for the
# HTTP surface LWA fronts. Does NOT include strands-agents or bedrock-agentcore
# SDKs; this Lambda only relays bytes from the AgentCore Runtime to the caller.
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.4
boto3==1.43.5

# Security-constrained transitive dependencies.
idna==3.15
starlette>=1.3.1
urllib3==2.7.0
